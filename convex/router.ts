import { httpRouter } from "convex/server";

const http = httpRouter();

// We can add HTTP routes here if needed
// For now, no routes are required since we're using Clerk directly

export default http;
